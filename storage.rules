
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // ATENÇÃO: Regra insegura temporária para diagnóstico.
    // Permite que qualquer pessoa leia e escreva em seu storage.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
