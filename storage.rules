
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allows public read and write access to all files.
    // WARNING: This is insecure and for diagnostic purposes only.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
