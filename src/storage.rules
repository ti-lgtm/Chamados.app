
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // REGRA DE DIAGNÓSTICO INSEGURA: Permite acesso total a qualquer pessoa.
    // Esta regra é apenas para um teste final.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
